variables.js
  Line 8:arrayのポジションは、必ず整数ではないといけないので0.5の値が入ってしまうとエラーになってしまいます。
  
js/array-sum.js
  Line 47:コメントで？いっぱいありますが解決出来ました？　コードは指摘無いです。
  
js/olympic-games.js
  ばっちりですね！プラスアルファで開催されなかった年が表示されないようにも分岐を入れていますね。
  一つアドバイスです。
  ifを書いたときに、ifの中が空っぽになってelseの中だけに処理が入っている場合は、条件を反転させてelseをなくすとすっきりします。
  例：if(!(count == 1916 || count == 1940 || count == 1944))
  
  あとは開催されていない年を定数のarrayで定義してその中に含まれていないかをチェックする、やり方などを試してみてください。
  開催されなかった年が増えたときにarrayに一つ数字を足すだけになるのでメンテが楽になります。
  
  例：const missYears = [1916, 1940, 1944]
      missYears.find(count) != count <- arrayの値を探し出す。
      missYears.indexOf(count) < 0　<- arrayのポジションを探す
  
  oop/class.js
    全体的に定義はできているかと思います。
    いくつか修正点があるので見直してみてください。
        
    Line 1:クラス名がRPGだと、これはゲーム？キャラクター？何を表したいのかわかりにくいので、名前を違うものにしてみてください。
    RPGクラスのattackはプロパティー（属性や特徴）ではないのでメソッド（できること、動作）として定義してください。
    Line 13, 23, 33, 43: 
      letの後の変数名は小文字で始めてください。
      せっかくYuusya, Wizar, Cleric, Berserkerクラスを定義したのにそれぞれのクラスを使わないのはもったいないです。
